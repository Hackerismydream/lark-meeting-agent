## ADDED Requirements

### Requirement: Scenario evidence packs

Each real Feishu demo SHALL write a local evidence pack under `runs/real_feishu_scenarios/`.

#### Scenario: Evidence artifacts

- **WHEN** a scenario completes or is blocked
- **THEN** it SHALL write command logs, local artifacts, trace evidence, and screenshot placeholders
